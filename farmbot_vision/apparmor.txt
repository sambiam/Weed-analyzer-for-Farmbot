#include <tunables/global>

profile farmbot_vision flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>

  network inet stream,
  network inet6 stream,
  network unix stream,

  /app/run.sh ix,
  /bin/dash ix,
  /bin/sh ix,
  /usr/local/bin/python3.12 ix,
  /usr/local/bin/python ix,
  # Python, NumPy, and OpenCV load shared objects from the complete
  # /usr/local/lib tree.  `m` is required for executable mmap of those
  # objects; without it the loader reports them as "not found".
  /usr/local/lib/** mr,
  /usr/lib/** mr,
  /lib/** mr,
  /usr/local/lib/python312.zip r,
  /etc/ssl/certs/** r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /proc/** r,
  /sys/fs/cgroup/** r,
  /sys/devices/system/cpu/** r,
  /app/** r,
  /data/** rwk,
  /tmp/** rwk,
  deny /proc/sys/** w,
}
