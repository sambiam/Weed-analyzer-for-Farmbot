#include <tunables/global>

profile farmbot_vision flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>

  network inet stream,
  network inet6 stream,
  network unix stream,

  /app/run.sh ix,
  /bin/dash ix,
  /bin/sh ix,
  /usr/local/bin/python3.12 ix,
  /usr/local/bin/python ix,
  /usr/local/lib/libpython3.12.so* mr,
  /usr/local/lib/python3.12/** mr,
  /usr/lib/** r,
  /lib/** r,
  /etc/ssl/certs/** r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /proc/** r,
  /sys/fs/cgroup/** r,
  /sys/devices/system/cpu/** r,
  /app/** r,
  /data/** rwk,
  /tmp/** rwk,
  deny /proc/sys/** w,
}
